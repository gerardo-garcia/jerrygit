---
import TagBadge from './TagBadge.astro';
import { formatDate, type Lang } from '../i18n/utils';

interface Props {
  title: string;
  description: string;
  date: Date;
  tags: string[];
  href: string;
  lang: Lang;
}
const { title, description, date, tags, href, lang } = Astro.props;
---

<article class="group term-card p-5 mb-3 last:mb-0">
  <a href={href} class="block">
    <time class="text-xs text-slate-500 font-mono">{formatDate(date, lang)}</time>
    <h2 class="mt-1 text-lg font-semibold text-slate-100 group-hover:text-emerald-300 transition-colors leading-snug">
      {title}
    </h2>
    <p class="mt-1 text-sm text-slate-400 leading-relaxed line-clamp-2">{description}</p>
    {tags.length > 0 && (
      <div class="mt-3 flex flex-wrap gap-1.5">
        {tags.map(tag => <TagBadge tag={tag} />)}
      </div>
    )}
  </a>
</article>
