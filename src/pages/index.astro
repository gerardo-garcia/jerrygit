---
const base = (import.meta.env.BASE_URL + '/').replace(/\/+$/, '/');
---
<meta http-equiv="refresh" content={`0;url=${base}en/`} />
<script define:vars={{ redirectUrl: base + 'en/' }}>window.location.replace(redirectUrl);</script>
